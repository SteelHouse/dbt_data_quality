
version: 2

models:
  - name: site_facts
    description: "A starter dbt model"
    columns:
      - name: advertiser_id
        description: this is the advertiser_id
        tests:
          - not_null

