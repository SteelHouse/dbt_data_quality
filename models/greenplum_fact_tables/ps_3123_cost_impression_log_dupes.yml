version: 2

models:
  - name: ps_3123_cost_impression_log_dupes
    description: "Check for that combo is unique"
    columns:
      - name: advertiser_id
        description: this is the advertiser_id
        tests:
          - not_null
      - name: campaign_id
        tests:
          - not_null
tests:
  - dbt_utils.unique_combination_of_columns:
      combination_of_columns:
        - advertiser_id
        - campaign_id
