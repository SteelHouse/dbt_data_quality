
version: 2

models:
  - name: impression_facts_vast_completes_clickhouse
    description: "A starter dbt model"
    columns:
      - name: advertiser_id
        description: this is the advertiser_id
        tests:
          - not_null
      - name: campaign_id
        tests:
          - not_null
