
version: 2

models:
  - name: all_facts_clickhouse
    description: "A starter dbt model"
    columns:
      - name: advertiser_id
        description: this is the advertiser_id
        tests:
          - not_null
      - name: campaign_id
        tests:
          - not_null
